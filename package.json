{
  "name": "mesh-to-svg",
  "version": "0.0.0-develop",
  "main": "index.js",
  "repository": "git@github.com:zakhenry/mesh-to-svg.git",
  "author": "Zak Henry <zak.henry@gmail.com>",
  "license": "MIT",
  "private": true,
  "scripts": {
    "wasm:build:release": "wasm-pack build --release -- --features console_log",
    "wasm:build:prod": "wasm-pack build --release",
    "wasm:build:debug": "wasm-pack build --debug -- --features console_log",
    "wasm:lint:fix": "cargo fix --allow-dirty --allow-staged && cargo fmt",
    "wasm:lint:check": "cargo check && cargo fmt -- --check"
  }
}
